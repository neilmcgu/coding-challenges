// merge sorted
// given two arrays that are already sorted from lowest to highest (non-decreasing order)
// merge them into a single array without creating anew array.
// the first array will have room for the second array on the end
