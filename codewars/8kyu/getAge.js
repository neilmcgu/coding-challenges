function getAge(inputString) {
  // return the girl's correct age as an integer. Happy coding :)
  let age = inputString.split(" ")[0];
  return Number(age);
}

// can also use parseint

const age = (inputString) => {
  parseInt(inputString);
};
